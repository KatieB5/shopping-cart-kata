const shoppingCartData = require("./data-sources/data-source-1.json");
const {products} = require("./products");
const {offers} = require("./offers");

const checkout = (shoppingCartData) => {
    
  return total;
};

checkout(shoppingCartData);

module.exports = checkout;
